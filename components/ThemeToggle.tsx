import React from 'react';

// This component is no longer used in the new multi-theme system.
// Returning null removes it from the component tree.
export const ThemeToggle: React.FC<any> = () => {
  return null;
};